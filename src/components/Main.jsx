export default function Main() {
    return (
        <form action="">
            <input aria-label="Add ingredient" type="text" placeholder="e.g. chicken broth" />
            <button>+ Add ingredient</button>
        </form>
    )
}